export default function Exercicio4({tipo, base, altura})
{ 
    let tipo = Number(tipo) + (area + base + altura *2(altura=base=tipo)) ;
     return(
    <div>
       A base é {base} a altura é {altura} {tipo.toFixed(2)}, e o tipo é {tipo}.
    </div>
);
}
